import React from 'react';

const ArticleBlock = () => {
    return (
        <section className={'articl-section'}>
            Article block
        </section>
    );
};

export default ArticleBlock;
